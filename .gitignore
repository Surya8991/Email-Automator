node_modules
.next
out
dist
build

# env / secrets
.env
.env.*.local

# DB / data
*.db
*.db-journal
data/
# …but DO ship the seed templates — onboarding loads them at first sign-in,
# and Vercel needs the file in the build to populate new users' libraries.
!data/seed-templates.json

# logs
*.log

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.pid

# test artifacts
playwright-report
test-results
coverage

# Sub-agent ephemeral scratch (created by code-review tooling)
.agentmaster/
